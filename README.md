# AI Risk Officer 2025 — интеллектуальная система оценки рисков контрагентов

Основной интерфейс проекта — **Web UI** для работы с агентной системой (A2A) и подключенными MCP-сервисами (ФНС/банки/арбитраж). Telegram-бот — **опциональный** канал доступа и вынесен в конец документа.

---

## Оглавление

- [Описание системы](#описание-системы)
- [URL (агенты и MCP)](#url-агенты-и-mcp)
- [Живое демо (Web UI)](#живое-демо-web-ui)
- [Веб-UI для взаимодействия с агентом](#веб-ui-для-взаимодействия-с-агентом)
- [Подключенные API](#подключенные-api)
- [Как развернуть в Cloud.ru (админам)](#как-развернуть-в-cloudru-админам)
- [Сервисы в Cloud.ru (MCP-серверы)](#сервисы-в-cloudru-mcp-серверы)
  - [mcp-server-fns](#mcp-server-fns)
  - [mcp-server-bank-statement](#mcp-server-bank-statement)
  - [mcp-server-kad-arbitr](#mcp-server-kad-arbitr)
- [Агентная система (A2A)](#агентная-система-a2a)
- [Промпты агентов (system prompts)](#промпты-агентов-system-prompts)
  - [agent_fns (ФНС)](#agent_fns-фнс)
  - [agent_bank_statement (банковские выписки)](#agent_bank_statement-банковские-выписки)
  - [agent_kad_arbitr (арбитраж)](#agent_kad_arbitr-арбитраж)
  - [AI Risk Officer 2025 (оркестратор / agent-system)](#ai-risk-officer-2025-оркестратор--agent-system)
- [Telegram-бот (опционально)](#telegram-бот-опционально)

---

## Описание системы

Проект объединяет **Web UI**, **agent-system (A2A)**, **A2A-агентов** и **MCP-сервисы** для задач российского бизнеса: налоги/комплаенс (ФНС), банковские выписки и арбитраж. Telegram-бот используется как дополнительный канал (опционально).

### Компоненты

- **Web UI (React + Vite + Tailwind)**: чат в стиле Telegram/ChatGPT, визуализация рисков, таблицы, превью PDF/XML, Markdown.
- **FastAPI backend (`ui_app.py`)**: REST API-прокси для отправки сообщений A2A-агенту из UI.
- **Agent System (оркестратор)**: `agent-system-usn-osno-nds-and-risc-score` — выбирает нужного агента под запрос и собирает итоговый отчёт.
- **A2A-агенты (3 шт.)**: `agent_fns`, `agent_bank_statement`, `agent_kad_arbitr`.
- **MCP-сервисы (3 шт.)** — запускаются в Cloud.ru как Container Apps и предоставляют инструменты агентам:
  - `fns-tax-mcp/` — генерация деклараций + методы api-fns.ru
  - `bank-statement-mcp/` — выписки по расчётному счёту (T‑Bank/Модульбанк/Альфа)
  - `kadarbitrmcp/` — поиск/детали/скачивание PDF арбитражных дел kad.arbitr.ru через api-assist
- **Telegram bot (`aiogram 3`) (опционально)**: чат-обёртка для общения с агентами по URL, retry, обработка голосовых сообщений, базовая телеметрия.

---

## URL (агенты и MCP)

| Тип | Название | URL |
|---|---|---|
| Agent System | agent-system-usn-osno-nds-and-risc-score | https://7f69ee58-fda6-4701-88c8-ec3ea67e4be1-agent-system.ai-agent.inference.cloud.ru |
| A2A agent | agent-kad-arbitr | https://0847fa4c-3d7c-4951-b618-ba5feadaeddf-agent.ai-agent.inference.cloud.ru |
| A2A agent | agent-fns | https://c8397491-7061-41aa-b82f-8cd7be888071-agent.ai-agent.inference.cloud.ru |
| A2A agent | agent-bank-statement | https://62d1854f-ce00-440f-80fb-5a8142361e1e-agent.ai-agent.inference.cloud.ru |
| MCP server | mcp-server-kad-arbitr | https://67e0acea-15bf-4034-9cd6-5f4940ff97e9-mcp-server.ai-agent.inference.cloud.ru/mcp |
| MCP server | mcp-server-fns | https://dc41f1b0-0a90-4834-90e1-7269d92aa1e4-mcp-server.ai-agent.inference.cloud.ru/mcp |
| MCP server | mcp-server-bank-statement | https://df98ce3e-e263-4c9a-a73e-d3527e5a4124-mcp-server.ai-agent.inference.cloud.ru/mcp |

---

## Живое демо (Web UI)

- **Web UI**: [http://176.109.106.231:8080/](http://176.109.106.231:8080/)
- **Видео-демо (mp4)**: [`Демо.mp4`](./Демо.mp4)

---

## Веб-UI для взаимодействия с агентом

Проект включает два варианта веб-интерфейса:

### 1. React Frontend (Рекомендуется)

Современный React-интерфейс с поддержкой:
- Чат-интерфейс в стиле Telegram/ChatGPT
- Отображение рисков с диаграммами (pie charts)
- Таблицы с данными (сортировка, поиск, пагинация)
- Превью и скачивание PDF/XML файлов
- Markdown-форматирование ответов
- Темная тема

**Запуск через Docker:**
```bash
# Сборка frontend образа
docker build -t ui-risk-agent:latest .

# Запуск контейнера
docker run -d --name ui-frontend -p 8080:80 \
  -e VITE_API_URL=http://127.0.0.1:8010/api/send \
  ui-risk-agent:latest
```

Frontend будет доступен на `http://localhost:8080` и автоматически подключится к backend API.

### 2. FastAPI Backend

Backend предоставляет REST API для взаимодействия с A2A агентом.

**Запуск:**
```bash
uvicorn ui_app:app --host 0.0.0.0 --port 8010 --reload
```

**Переменные окружения (обязательные):**
- `PUBLIC_URL` — URL A2A агента (например, `https://your-agent-id-agent-system.ai-agent.inference.cloud.ru`)
- `AGENT_TOKEN` — Bearer токен для авторизации

**API Endpoints:**
- `GET /` — простая HTML форма для тестирования
- `POST /api/send` — отправка сообщения агенту
- `GET /health` — проверка здоровья сервиса

**Важно:** Все токены и URL должны быть указаны в `.env` файле. Хардкодные значения удалены из кода.

---

## Подключенные API

Ниже перечислены внешние API/источники данных, к которым обращаются MCP-сервисы (напрямую или через прокси), и ссылки на их документацию.

### ФНС / ЕГРЮЛ / ЕГРИП (через API-ФНС)

- **Документация**: [api-fns.ru — описание API](https://api-fns.ru/api_help)
- **Что даёт**: интеграция данных ФНС (ЕГРЮЛ/ЕГРИП), поиск/проверки, выписки, бухотчётность, статусы физлиц; ответы в формате JSON.  
  Единая точка входа (по документации): `https://api-fns.ru/api`.
- **Где используется**: MCP `mcp-server-fns` (папка `fns-tax-mcp/`).

### Арбитраж (kad.arbitr.ru) через api-assist.com

- **Источник карточек дел**: `https://kad.arbitr.ru/`
- **Документация API прокси**: `https://api-assist.com/documentation/arbitr-api.txt`
- **Где используется**: MCP `mcp-server-kad-arbitr` (папка `kadarbitrmcp/`) — инструменты `arbitr_search_cases`, `arbitr_details_by_number`, `arbitr_details_by_id`, `arbitr_download_pdf`.

### Банки (выписки по расчётному счёту)

- **Модульбанк API**: `https://api.modulbank.ru/`
- **T‑Bank (developer docs)**: `https://developer.tbank.ru/docs/intro/integration-steps`
- **Альфа-Банк (manual)**: `https://partner.alfabank.ru/files/download?code=api-manual`
- **Где используется**: MCP `mcp-server-bank-statement` (папка `bank-statement-mcp/`) — инструмент `get_bank_statement`.

### Документы / презентации

- `AI-RISK-OFFICER-2025-Intellektualnaya-sistema-ocenki-riskov-kontragentov.pdf` — презентация системы (agent-system + агенты + MCP).

---

## Как развернуть в Cloud.ru (админам)

Этот раздел написан так, чтобы администратор Cloud.ru мог **воспроизвести стенд**: поднять MCP-сервисы, собрать A2A-агентов, оркестратор (agent-system) и подключить к ним Web UI / Telegram-бот.

### 1) Развернуть MCP-сервисы (Container Apps)

1. Создайте 3 Container Apps (или аналогичные сервисы) из образов:
   - `mcp-server-fns` → образ `tax-fns-agents-registry.cr.cloud.ru/fns-tax-mcp@sha256:a485a8d1beee016fae20b323dd5ccc5ebc10363e9b742c9f66d028ce58583cb6`
   - `mcp-server-bank-statement` → образ `tax-fns-agents-registry.cr.cloud.ru/bank-statement-mcp@sha256:b8cdf83addcd857612dae6e7676cc8a6f86b9afb501d4218055128b87b192df1`
   - `mcp-server-kad-arbitr` → образ `tax-fns-agents-registry.cr.cloud.ru/mcp-arbitr@sha256:5ab16e447df27dd77fee7f04c91cf788e422488aaeb3445ae2b59e968dd4076b`
2. Примените общие параметры из раздела [Сервисы в Cloud.ru (MCP-серверы)](#сервисы-в-cloudru-mcp-серверы).
3. Задайте переменные окружения:
   - **FNS**: `FNS_MODE`, `FNS_API_TOKEN` (секрет)
   - **Bank statement**: `MODE`, и нужный токен банка (`T_BANK_TOKEN` / `MODULBANK_TOKEN` / `ALFA_TOKEN`) как секрет
   - **Kad arbitr**: `ARBITR_MODE`, `ARBITR_API_KEY` (секрет, обязателен в `prod`)
4. Опубликуйте публичные URL MCP endpoint (обычно `/mcp`) и сохраните их — они понадобятся при создании A2A-агентов.

### 2) Проверить работоспособность MCP

- Проверьте `GET /health` каждого MCP-сервиса (должен отвечать `200 OK`).
- Проверьте, что MCP endpoint доступен по URL вида `https://.../mcp`.

### 3) Создать A2A-агентов и привязать MCP

Создайте 3 A2A-агента:

- `agent_fns` → подключите MCP `mcp-server-fns`
- `agent_bank_statement` → подключите MCP `mcp-server-bank-statement`
- `agent_kad_arbitr` → подключите MCP `mcp-server-kad-arbitr`

В `system prompt` каждого агента вставьте промпт из раздела [Промпты агентов (system prompts)](#промпты-агентов-system-prompts).

### 4) Создать agent-system (оркестратор)

Создайте agent-system `agent-system-usn-osno-nds-and-risc-score` и вставьте промпт оркестратора из раздела [AI Risk Officer 2025 (оркестратор / agent-system)](#ai-risk-officer-2025-оркестратор--agent-system).

### 5) Подключить Web UI / Telegram-бот к agent-system

В этом репозитории UI и бот используют:

- `PUBLIC_URL` = URL agent-system (или конкретного агента)
- `AGENT_TOKEN` = Bearer токен A2A (если включена авторизация)

Далее запускайте UI и/или бота по разделам [Веб-UI для взаимодействия с агентом](#веб-ui-для-взаимодействия-с-агентом) и [Пошаговый гайд](#пошаговый-гайд).

---

## Сервисы в Cloud.ru (MCP-серверы)

Ниже — описание MCP-сервисов в терминах Cloud.ru (чтобы администратор мог воспроизвести конфигурацию и запустить у себя).

### Общие параметры (рекомендуемые)

- **Конфигурация**: 1 vCPU / 2 GB RAM
- **Масштабирование**: RPS, 200 req/s, min=1, max=1
- **Запускать дочерние контейнеры при запросе**: включено
- **Аутентификация**: выключено (при необходимости включите на стороне Cloud.ru)
- **Логирование запросов**: включено (лог-группа `default`)
- **Файловая система**: выключено (соответствует требованиям AI Agents Cloud.ru)

### mcp-server-fns

- **Назначение**: единая точка доступа к инструментам ФНС (api-fns.ru) + генерация деклараций (УСН/ОСНО/НДС/6-НДФЛ) в XML.
- **URI образа (Artifact Registry)**: `tax-fns-agents-registry.cr.cloud.ru/fns-tax-mcp@sha256:a485a8d1beee016fae20b323dd5ccc5ebc10363e9b742c9f66d028ce58583cb6`
- **Переменные окружения**:
  - `FNS_MODE` = `prod` | `test` | `free`
  - `FNS_API_TOKEN` = токен api-fns.ru (**секрет**)
- **Инструменты (tools)**:
  - Генерация деклараций: `generate_usn_declaration`, `generate_osno_declaration`, `generate_nds_declaration`, `generate_6ndfl_declaration`
  - Компании/контрагенты/контроль: `search_companies`, `autocomplete`, `get_company_data`, `multinfo_companies`, `multcheck_companies`, `check_counterparty`, `track_changes`, `monitor_companies`
  - Выписки/отчётность/лицензии: `get_extract`, `get_msp_extract`, `get_accounting_report`, `get_accounting_report_file`, `get_fsrar_licenses`
  - Физлица/паспорта: `get_inn_by_passport`, `check_passport`, `check_passport_info`, `check_person_status`
  - Блокировки/статистика: `check_account_blocks`, `check_account_blocks_file`, `get_api_statistics`

> Локальная разработка/запуск: см. `fns-tax-mcp/README.md` и `fns-tax-mcp/docker-compose.yml`.

### mcp-server-bank-statement

- **Назначение**: получение выписок операций по расчётному счёту за период (T‑Bank, Модульбанк, Альфа-Банк).
- **URI образа (Artifact Registry)**: `tax-fns-agents-registry.cr.cloud.ru/bank-statement-mcp@sha256:b8cdf83addcd857612dae6e7676cc8a6f86b9afb501d4218055128b87b192df1`
- **Переменные окружения**:
  - `MODE` = `prod` | `test`
  - `T_BANK_TOKEN` / `MODULBANK_TOKEN` / `ALFA_TOKEN` (**секреты**, в зависимости от банка)
- **Инструменты (tools)**: `get_bank_statement`

> Локальная разработка/запуск: см. `bank-statement-mcp/README.md` и `bank-statement-mcp/docker-compose.yml`.

### mcp-server-kad-arbitr

- **Назначение**: поиск/детализация арбитражных дел kad.arbitr.ru и скачивание судебных актов в PDF (через api-assist.com).
- **URI образа (Artifact Registry)**: `tax-fns-agents-registry.cr.cloud.ru/mcp-arbitr@sha256:5ab16e447df27dd77fee7f04c91cf788e422488aaeb3445ae2b59e968dd4076b`
- **Переменные окружения**:
  - `ARBITR_MODE` = `test` | `prod`
  - `ARBITR_API_KEY` = ключ api-assist.com (**секрет**, обязателен в `prod`)
- **Инструменты (tools)**:
  - `arbitr_search_cases`
  - `arbitr_details_by_number`
  - `arbitr_details_by_id`
  - `arbitr_download_pdf`

> Локальная разработка/запуск: см. `kadarbitrmcp/README.md`.

---

## Агентная система (A2A)

В Cloud.ru дополнительно развёрнута **agent-system** (оркестратор), который **не вызывает MCP напрямую**, а работает через 3 A2A-агента:

- `agent_fns` — налоговый/комплаенс агент (работает через MCP `mcp-server-fns`)
- `agent_bank_statement` — банковские выписки (работает через MCP `mcp-server-bank-statement`)
- `agent_kad_arbitr` — арбитраж (работает через MCP `mcp-server-kad-arbitr`)

Веб-UI и Telegram-бот подключаются к **agent-system** (или к конкретному агенту) через `PUBLIC_URL` + `AGENT_TOKEN`.

---

## Промпты агентов (system prompts)

Ниже — промпты, которые используются для конфигурации агентов/agent-system в Cloud.ru. Их можно копировать как `system prompt` при создании агентов.

### agent_fns (ФНС)

<details>
<summary>Показать промпт</summary>

Ты — профессиональный налоговый и комплаенс-ассистент для российского бизнеса. Ты работаешь исключительно через доступные MCP-инструменты (обёртки над api-fns.ru) и НИКОГДА не придумываешь данные сам. Твоя задача — максимально точно и быстро выполнить любой запрос пользователя, связанный с: генерацией налоговых деклараций (УСН, ОСНО, НДС, 6-НДФЛ), проверкой контрагентов и физлиц, получением выписок, бухотчётности, лицензий ФСРАР, проверкой блокировок счетов и паспортов, мониторингом изменений в ЕГРЮЛ/ЕГРИП.

ПРАВИЛА РАБОТЫ (соблюдай строго):

Всегда анализируй запрос и используй минимально необходимые инструменты, чтобы избежать задержек.
Вызывай инструменты последовательно, шаг за шагом, обрабатывая ответы перед следующим вызовом.
Не вызывай лишние инструменты (например, для базовой информации по ИНН — только search_companies + get_company_data, без полной цепочки проверки).
Если нужны данные — сначала найди компанию через search_companies или autocomplete, затем используй get_company_data или check_counterparty.
Для генерации деклараций (УСН, НДС, 6-НДФЛ и т.д.) — обязательно запрашивай у пользователя недостающие параметры (доходы, расходы, период, ставка и т.д.), если они не указаны. Если всё указано — сразу вызывай соответствующий инструмент: generate_usn_declaration для УСН, generate_osno_declaration для ОСНО, generate_nds_declaration для НДС, generate_6ndfl_declaration для 6-НДФЛ. После вызова — выводи полный содержимый сгенерированного файла (XML/PDF) в текстовой форме или таблице.
Для выписок — всегда вызывай get_extract или get_msp_extract, затем выводи полный текст выписки в структурированной форме (таблица с разделами: статус, адрес, учредители, налоги и т.д.).
Для полной проверки контрагента — search_companies → get_company_data → check_counterparty → check_account_blocks. Выводи ВСЕ данные: статус, долги (с суммами и типами), блокировки (с датами и причинами), лицензии (полный список).
Если нужна бухотчётность — get_accounting_report_file (с xls=True для Excel), затем выводи таблицу с ключевыми показателями (доходы, расходы, прибыль).
При массовой проверке — используй multcheck_companies (проверь лимит через stat).
Для рисков: Выводи МАКСИМУМ деталей — таблица с налогами/долгами/пенями/штрафами (суммы, даты), статус банкротства/недостоверности, блокировки счетов (список), изменения в ЕГРЮЛ (хронология). Затем — финальное резюме с риском (низкий/средний/высокий) и рекомендацией.
Все ответы пиши на русском языке, структурированно:
Детальный отчёт (таблицы, списки с эмодзи: ✅ для ок, ⚠️ для предупреждений, 🛑 для рисков).
Файлы (указывай, какие сгенерированы: XML, PDF, ZIP, и давай прямые ссылки или текст содержимого).
Резюме (краткое: риски, рекомендации).
Подсвечивай риски: 🛑 для долгов >100к руб., ⚠️ для недостоверных сведений, ✅ для чистого статуса.
Ты НЕ бухгалтер, НЕ даёшь налоговых консультаций, НЕ интерпретируешь законы. Ты — исполнительный инструмент, который мгновенно генерирует документы и проверки через официальные источники ФНС.
Отвечай кратко, по делу, в деловом стиле. Если всё готово — начинай с: "Готово. Вот детальный отчёт:" и заканчивай резюме. Ты — лучший налоговый робот в России. Работай идеально.

</details>

### agent_bank_statement (банковские выписки)

<details>
<summary>Показать промпт</summary>

Ты — банковский ассистент для бизнеса. Твоя единственная функция — мгновенно выдавать выписки операций по расчётным счетам в Т‑Банке, Модульбанке и Альфа-Банке.

У тебя есть ровно один инструмент: get_bank_statement.

ПРАВИЛА РАБОТЫ (соблюдай строго):

Никогда не придумывай операции и не пиши «я не могу получить выписку». У тебя всегда есть доступ.
Всегда используй только инструмент get_bank_statement — других инструментов у тебя нет.
Перед вызовом инструмента обязательно уточни у пользователя:
За какой период нужна выписка (с какой по какую дату в формате ГГГГ-ММ-ДД).
Банк (Т‑Банк, Модульбанк, Альфа-Банк).
Номер счёта или account_id (если несколько — уточни, иначе основной).
После получения результата от MCP сразу выдавай пользователю полный человекочитаемый отчёт на русском языке:
Детальный отчёт:
Указывай банк, период, номер счёта.
Таблица всех операций (Markdown-таблица: | Дата | Описание | Сумма | Остаток | — с эмодзи: 📈 для поступлений, 📉 для списаний).
Подсвечивай риски: 🛑 для подозрительных операций (если сумма >1млн без описания), ⚠️ для отрицательного остатка.
Файлы: Указывай, если сгенерирован PDF/XML, и давай прямую ссылку или текст содержимого.
Резюме: Итоговое сальдо на конец периода, общий оборот (+/- суммы), рекомендации (например, "Оборот стабильный ✅" или "Обнаружены крупные списания ⚠️").
Форматируй ответ только в Markdown с использованием таблиц и эмодзи.
Если пользователь пишет «выписка за ноябрь», «покажи движения по счёту», «что было на счёте в декабре» — сразу уточняй даты/банк/счёт и вызывай инструмент.
В тестовом режиме (mode = test) обязательно пиши в конце отчёта маленьким серым текстом: Это тестовая выписка (режим test). Реальные данные будут после подключения токенов.
Если данных много — разбивай таблицу на страницы или суммируй, но выводи ВСЕ операции.
Ты не консультируешь, не считаешь налоги, не анализируешь — только выдаёшь выписки быстро и красиво. Отвечай максимально кратко и по делу, начиная с: "Готово! Вот детальная выписка:" и заканчивая резюме.

Ты — самый быстрый и надёжный банковский выписочник в России. Работай идеально.

Пример правильного ответа:

Готово! Выписка Т‑Банк  
Период: 01.11.2025 – 30.11.2025  
Счёт: ∙∙∙∙001  
  
Дата\tОписание\tСумма\tОстаток  
01.11.2025 10:00\t📈 Поступление от ООО «Ромашка»\t+150 000 ₽\t265 000 ₽  
15.11.2025 14:20\t📉 Оплата поставщику\t–87 500 ₽\t177 500 ₽  
Итого оборот: +62 500 ₽  
Резюме: Сальдо положительное ✅, без рисков.  
Это тестовая выписка (режим test). Реальные данные будут после подключения токенов.

</details>

### agent_kad_arbitr (арбитраж)

<details>
<summary>Показать промпт</summary>

Ты — лучший в России AI-арбитражный аналитик «Арбитражный Сканер 2025». Ты работаешь только через mcp-server-kad-arbitr и мгновенно находишь, анализируешь арбитражные дела из kad.arbitr.ru. У тебя четыре мощных инструмента: • arbitr_search_cases — поиск дел по ИНН, названию, роли, датам, суду и типу дела. • arbitr_details_by_number — полные карточки дел по номеру (А40-12345/2024). • arbitr_details_by_id — карточки по внутреннему ID. • arbitr_download_pdf — скачивает любой судебный акт в PDF и возвращает РЕАЛЬНУЮ ссылку на файл.

ПРАВИЛА РАБОТЫ — соблюдай свято:

НИКОГДА не придумывай дела, суммы, даты, статусы или любые данные. Используй ТОЛЬКО реальные данные из результатов инструментов. Если данных нет (например, сумм не указано) — пиши "не указана" или "данные отсутствуют". Если инструмент вернул 3 дела — работай только с ними, не добавляй вымышленные.
Для любого запроса: Сначала вызови arbitr_search_cases с параметрами (Inn, DateFrom, DateTo). Если meta['pages'] >1 — вызови инструмент последовательно для каждой страницы (page=1, затем page=2 и т.д.), собери ВСЕ Cases из structured_content в один список перед анализом.
Парси structured_content: Статистику считай САМ из списка Cases (total = len(Cases), plaintiff = count где роль истец, respondent = count где ответчик, in_progress = count где статус 'В работе' или аналог, Σ требований = sum сумм из Cases, если сумм нет — 0).
Не вызывай arbitr_details_by_number, arbitr_details_by_id или arbitr_download_pdf автоматически — это приводит к таймаутам. Вызывай ТОЛЬКО если пользователь explicitly просит детали по конкретному делу или скачивание. Для PDF: arbitr_download_pdf — получи РЕАЛЬНУЮ ссылку на файл и укажи \"Скачать PDF\".
Риск-скор арбитража (0–100) — считай сам СТРОГО на основе реальных Cases: • > 20 дел всего → +40 • > 50 дел всего → +70 • > 5 дел как ответчик (роль respondent) за последние 12 мес (проверь StartDate) → +30 • Сумма требований > 10 млн руб (суммируй реальные суммы из Cases, если нет — +0) → +20 • Дела по 159, 160, 165 УК РФ (проверь по типу дела или тексту в Cases) → +50 • Красная зона = 80+ (🛑), Жёлтая = 40–79 (⚠️), Зелёная = 0–39 (✅). • Объясни расчёт: \"Риск-скор: [число] (🛑) = +40 (>20 дел) +0 (суммы не указаны) +...\".
Обязательный финальный отчёт в красивом Markdown: • Название компании + ИНН (возьми из первого Case или запроса). • Статистика: всего дел / как истец / как ответчик / в работе / Σ требований (реальные, если нет — \"не указана\"). • Таблица топ-5 по сумме (сортируй по суммам из Cases, если сумм нет — по дате; столбцы: | № | Дата | Роль | Суд | Сумма | Статус | — подсветка 🛑 для >1млн, если сумма не указана — \"не указана\"). • Таблица последних 10 дел (сортируй по StartDate desc; если <10 — все). • Файлы: Если вызван arbitr_download_pdf — \"Скачать PDF\". • Резюме: Риск-скор (с объяснением), краткий вывод одной строкой: «Чисто» / «Осторожно» / «Красная зона — не работать».
Всегда используй эмодзи, таблицы, выделение жирным и цветные блоки: [!NOTE] Для деталей дела укажите номер!
Если запрос на детали: arbitr_search_cases (если нужно подтвердить) → arbitr_details_by_number → если PDF нужен → arbitr_download_pdf и дай прямую ссылку + текст содержимого.
При необходимости сопоставь регион ИНН с картой судов; если нет карты — не фильтруй.

</details>

### AI Risk Officer 2025 (оркестратор / agent-system)

<details>
<summary>Показать промпт</summary>

Ты — AI Risk Officer 2025. Работаешь только через три A2A-агента:
- Налоговый → agent_fns
- Арбитражный → agent_kad_arbitr
- Банковский → agent_bank_statement
Никаких MCP.

Цель: подобрать и ВЫЗВАТЬ нужные агенты/инструменты для ЛЮБОГО запроса по ИНН/ОГРН/паспорту (риски, базовая инфо, адрес, статус, декларации, выписки). Если запрос на базовую инфо (адрес, реквизиты, статус) — используй agent_fns с search_companies/get_company_data. Выводи ЧЕЛОВЕКОЧИТАЕМЫЙ отчёт (НЕ JSON!): детальные блоки по ФНС/Арбитраж/Банк, таблицы, подсветка рисков (🛑/⚠️/✅), затем интегральный риск (ТОЛЬКО если запрос на риски).

Шаги:
1) Понять задачу: цель, субъект (ООО/ИП/физлицо), данные.
2) Выбрать и вызвать агентов последовательно (send_message с точным запросом).
3) Если данных не хватает — один короткий уточняющий вопрос.
4) После вызовов — свести результаты в структурированный отчёт.

Формат ответа (только русский, кратко, без «воды»):
- Всегда начинай с "Готово. Вот [данные/детальный анализ рисков]:" и заверши резюме.
- Не выводи пустые разделы/элементы (диаграмма/таблицы только если данные).
- Минимум эмодзи; только для подсветки (⚠️, ✅, 🛑).

Проверка доступности агентов: send_message с "Проверка доступности агента. Ответьте только: 'Доступен' или 'Недоступен'."

</details>

## Telegram-бот (опционально)

Ниже — инструкция по Telegram-боту. Если вам нужен только Web UI, этот раздел можно пропустить.

---

## Важные моменты (Telegram-бот)

### 1. Назначение бота
Бот позволяет интегрировать A2A агента в Telegram, обеспечивая:
- Старт и меню: удобное меню с разделами «Начать работу», «Помощь», «Подключиться по URL»
- Подключение к агенту по URL: вводите URL — и бот подключится к вашему агенту
- Общение с агентом: пересылайте текстовые запросы и получайте ответы
- Обработку голосовых сообщений: поддержка транскрипции голосовых сообщений
- Повтор запроса (Retry): в случае ошибки можно быстро повторить запрос
- История ошибок: краткая сводка последних ошибок для отладки

### 2. Требования
- Python 3.13 или выше
- [Poetry](https://python-poetry.org/) - менеджер зависимостей
- Доступ к Telegram Bot API (токен от BotFather)
- Доступ к A2A агенту
- OpenTelemetry для телеметрии (опционально)

---

## Пошаговый гайд (Telegram-бот)

### Шаг 1. Подготовка окружения

#### 1.1 Установка Poetry

```bash
# На Linux/Mac:
curl -sSL https://install.python-poetry.org | python3 -

# На Windows:
(Invoke-WebRequest -Uri https://install.python-poetry.org -UseBasicParsing).Content | python -

# Или через pip:
pip install poetry
```

#### 1.2 Клонирование и подготовка проекта

```bash
# Перейдите в директорию проекта
cd telegram-bot-for-a2a
```

#### 1.3 Установка зависимостей

```bash
# Установка зависимостей
poetry install --only main

# Или с dev зависимостями
poetry install
```

---

### Шаг 2. Настройка переменных окружения

#### 2.1 Создание .env файла

Создайте файл `.env` на основе примера:

```bash
cp .env.example .env
```

#### 2.2 Получение токена Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен
5. Вставьте токен в переменную `TELEGRAM_BOT_TOKEN` в `.env` файле

#### 2.3 Настройка A2A агента

1. Убедитесь, что у вас есть доступ к A2A агенту
2. Получите URL агента (например, `https://7d0939e3-38e1-4f8d-a0a2-773629701cde-agent.ai-agent.inference.cloud.ru`)
3. Заполните `PUBLIC_URL` в `.env` файле

#### 2.4 Пример .env файла

```bash
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_BOT_USERNAME=your_bot_username

# A2A Agent Configuration (ОБЯЗАТЕЛЬНО для веб-UI)
PUBLIC_URL=https://your-agent-id-agent-system.ai-agent.inference.cloud.ru
AGENT_TOKEN=your-agent-bearer-token

# Frontend API URL (для React приложения)
VITE_API_URL=http://127.0.0.1:8010/api/send

# Optional: Message handling
HANDLE_MESSAGE_EDITS=true
HANDLE_MESSAGE_DELETES=false
EDIT_RESPONSE_TIMEOUT=30

# Optional: OpenTelemetry
# OTEL_EXPORTER_OTLP_ENDPOINT=https://your-otel-endpoint
# OTEL_SERVICE_NAME=agent-ui
```

**Важно:** 
- `PUBLIC_URL` и `AGENT_TOKEN` обязательны для работы веб-UI
- Все токены должны быть указаны в `.env` файле, не в коде
- Создайте `.env` на основе `.env.example` (если существует) или используйте пример выше

---

### Шаг 3. Запуск бота

#### 3.1 Локальный запуск (без Docker)

```bash
# Запуск бота через Poetry
poetry run python main.py

# Или активируйте виртуальное окружение
poetry shell
python main.py
```

#### 3.2 Запуск в Docker

В проекте есть готовый `Dockerfile` (многоэтапная сборка с Poetry).

**Сборка образа:**

```bash
docker build -t tg-bot:latest .
```

**Запуск контейнера с переменными среды:**

```bash
docker run --rm \
  -e TELEGRAM_BOT_TOKEN=$TELEGRAM_BOT_TOKEN \
  -e TELEGRAM_BOT_USERNAME=$TELEGRAM_BOT_USERNAME \
  -e PUBLIC_URL=$PUBLIC_URL \
  tg-bot:latest

# или с .env файлом
docker run --rm --env-file .env tg-bot:latest
```

#### 3.3 Запуск через docker-compose

Создайте `docker-compose.yaml`:

```yaml
version: '3.8'

services:
  telegram-bot:
    build: .
    container_name: telegram-a2a-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
```

Запуск:

```bash
docker-compose up -d
docker-compose logs -f
```

---

### Шаг 4. Проверка работы

1. **Проверьте логи** - бот должен вывести сообщения об успешном запуске:
   ```
   Bot started
   Bot started with request manager
   ```

2. **Проверьте бота в Telegram**:
   - Найдите вашего бота в Telegram
   - Отправьте команду `/start`
   - Бот должен ответить приветственным сообщением с меню

3. **Проверьте подключение к агенту**:
   - Нажмите "🚀 Начать работу" → "🔌 Подключиться к чату"
   - Бот должен подключиться к агенту и показать статус

4. **Проверьте общение с агентом**:
   - Отправьте текстовое сообщение боту
   - Бот должен переслать его агенту и вернуть ответ

---

### Шаг 5. Использование

#### Команды бота

- `/start` - показать главное меню
- Остальные функции доступны через кнопки меню

#### Основные функции

1. **Подключение к агенту**:
   - Нажмите "🚀 Начать работу" → "🔌 Подключиться к чату"
   - Бот автоматически подключится к агенту по URL из конфигурации

2. **Общение с агентом**:
   - После подключения отправьте текстовое сообщение
   - Бот перешлет его агенту и вернет ответ

3. **Голосовые сообщения**:
   - Отправьте голосовое сообщение боту
   - Бот транскрибирует его и отправит агенту

4. **Повтор запроса**:
   - Если произошла ошибка, появится кнопка "🔄 Retry"
   - Нажмите её для повторной попытки

---

## Конфигурация

Все настройки выполняются через переменные окружения:

| Переменная | Описание | Обязательно |
|-----------|----------|-------------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от BotFather (для Telegram бота) | Да* |
| `TELEGRAM_BOT_USERNAME` | Юзернейм бота (без `@`) (для Telegram бота) | Да* |
| `PUBLIC_URL` | URL A2A агента | Да |
| `AGENT_TOKEN` | Bearer токен для авторизации в A2A агенте | Да |
| `VITE_API_URL` | URL backend API для React frontend | Нет (по умолчанию: `http://127.0.0.1:8010/api/send`) |
| `HANDLE_MESSAGE_EDITS` | Обрабатывать измененные сообщения (по умолчанию: true) | Нет |
| `HANDLE_MESSAGE_DELETES` | Игнорировать удаленные сообщения (по умолчанию: false) | Нет |
| `EDIT_RESPONSE_TIMEOUT` | Таймаут для отмены старого запроса в секундах (по умолчанию: 30) | Нет |

*Обязательно только для Telegram бота, не требуется для веб-UI

Опционально можно настраивать переменные для OpenTelemetry (например, `OTEL_EXPORTER_OTLP_ENDPOINT` и др.), если требуется экспорт трейсов. В коде уже используются трейсы (`opentelemetry`), но экспорт по умолчанию не настраивается.

---

## Телеметрия

Бот использует OpenTelemetry для трейсинга:
- Traces для всех запросов к агенту
- Spans с метаданными (user_id, message_id, response_time)
- Метрики производительности

Для экспорта трейсов настройте переменные окружения OpenTelemetry:
- `OTEL_EXPORTER_OTLP_ENDPOINT` - endpoint для экспорта
- `OTEL_SERVICE_NAME` - имя сервиса (по умолчанию: telegram-bot)

---

## Устранение неполадок

### Бот не запускается

1. Проверьте, что заданы `TELEGRAM_BOT_TOKEN` и `TELEGRAM_BOT_USERNAME`
2. Убедитесь, что токен корректен
3. Проверьте логи на наличие ошибок

### Нет ответа от агента

1. Убедитесь, что введён корректный URL агента в `PUBLIC_URL`
2. Проверьте, что указан правильный `AGENT_TOKEN` в `.env`
3. Проверьте, что агент доступен по сети
4. Проверьте логи на ошибки подключения
5. **Важно:** Все токены должны быть в `.env` файле, не в коде

### Ошибки с зависимостями

1. Выполните `poetry lock --no-update && poetry install`
2. Или пересоберите Docker-образ
3. Убедитесь, что используется правильная версия Python (3.13+)

### Проблемы с SSL/сертификатами

1. В Docker-образ добавлены `ca-certificates`
2. Если агент с самоподписанным сертификатом — проверьте доверие сертификату
3. Для отладки можно использовать HTTP вместо HTTPS

### Проблемы с голосовыми сообщениями

1. Убедитесь, что установлены зависимости для обработки аудио
2. Проверьте доступность сервисов транскрипции (Whisper, Google Speech)
3. Проверьте логи на ошибки обработки голосовых сообщений

---

## Структура проекта

```
crewairiskanaalizesystem/
├── main.py                 # Точка входа Telegram бота
├── ui_app.py              # FastAPI backend для веб-UI
├── Dockerfile              # Docker образ для React frontend
├── docker-compose.yaml     # Docker Compose конфигурация
├── pyproject.toml          # Конфигурация проекта и зависимости
├── poetry.lock             # Lock файл зависимостей (Poetry)
├── uv.lock                 # Lock файл зависимостей (uv)
├── .env.example            # Пример конфигурации (создайте .env на его основе)
├── .dockerignore           # Исключения для Docker build
├── README.md              # Документация
├── frontend/               # React frontend приложение
│  ├── src/
│  │  ├── App.tsx          # Главный компонент
│  │  ├── components/      # React компоненты (ChatMessage, DataTable, RiskChart)
│  │  ├── lib/             # Утилиты (parser.ts)
│  │  └── types.ts         # TypeScript типы
│  ├── package.json        # Frontend зависимости
│  └── vite.config.ts      # Vite конфигурация
├── config/
│  ├── nginx.conf          # Nginx конфигурация для frontend
│  └── config.py           # Конфигурация через pydantic-settings
└── src/
   ├── config/
   │  ├── __init__.py
   │  └── config.py         # Конфигурация через pydantic-settings
   ├── handlers/
   │  ├── __init__.py
   │  └── common.py         # Обработчики команд и сообщений
   ├── keyboards/
   │  └── __init__.py       # Клавиатуры для меню
   ├── models/
   │  └── __init__.py
   ├── services/
   │  ├── __init__.py
   │  ├── agent_connector.py # Интеграция с A2A агентом
   │  └── request_manager.py # Управление запросами
   └── utils/
      ├── __init__.py
      ├── commands.py        # Команды бота
      ├── session.py         # Управление сессиями
      └── voice_processor.py # Обработка голосовых сообщений
```

---

## Разработка

### Настройка окружения для разработки

```bash
# Установка с dev-зависимостями
poetry install

# Форматирование кода
poetry run black src

# Тесты (если будут добавлены)
poetry run pytest -q

# Запуск бота из Poetry
poetry run python main.py
```

### Технологии

**Backend:**
- **Python 3.12+**
- **FastAPI** (веб-фреймворк для API)
- **aiogram 3** (Telegram Bot API)
- **uvicorn** (ASGI сервер)
- **httpx** (HTTP-клиент)
- **a2a** (интеграция с A2A агентами)
- **pydantic-settings** (конфигурация через `.env`)
- **opentelemetry** (встроенные трейсы в коде; экспорт при наличии конфигурации)

**Frontend:**
- **React 18** (TypeScript)
- **Vite** (сборщик)
- **Tailwind CSS** (стилизация)
- **Chart.js** (диаграммы рисков)
- **react-table** (таблицы с данными)
- **react-markdown** (форматирование ответов)
- **react-pdf** (просмотр PDF)

Полезные ссылки:
- [Документация aiogram](https://docs.aiogram.dev)
- [Poetry (dependency management)](https://python-poetry.org/docs/)
- [OpenTelemetry Python](https://opentelemetry.io/docs/languages/python/)

---

## Резюме

* Бот интегрирует A2A агента в Telegram
* Поддерживает текстовые и голосовые сообщения
* Механизм повторных попыток при ошибках
* Полная телеметрия через OpenTelemetry
* Основной процесс: **настроить → запустить → использовать**
