# bank-statement-mcp — Универсальный банковский коннектор

Первый коробочный MCP-сервер в каталоге Cloud.ru, который позволяет **любому AI-агенту** получать банковские выписки из 3 ключевых банков России.

## 🎯 Что это даёт

- **Экономия времени**: Получение выписки за 10 секунд вместо ручного экспорта из личного кабинета
- **Безопасность**: Никаких логинов и паролей — только личный токен из ЛК банка
- **Универсальность**: Один MCP-сервер для 3 банков (T‑Bank, Модульбанк, Альфа-Банк)
- **Соответствие 152-ФЗ**: Токены выдаются банком, передача паролей не требуется

## 👥 Для кого

- **Финансовые директора**: Автоматический анализ операций по счетам
- **Менеджеры по закупкам**: Контроль расходов и планирование бюджета
- **Юристы**: Подготовка документов с банковскими выписками
- **Бухгалтеры**: Интеграция выписок в системы учёта

## 🚀 Как подключить за 10 секунд

1. Добавьте этот MCP в ваш агент в Evolution AI Agents
2. Заполните два поля:
   - `BANK_PROVIDER` → `tbank` | `modulbank` | `alfa`
   - Соответствующий токен (из личного кабинета банка):
     - **T‑Bank (бывш. Тинькофф)**: ЛК → Интеграции → API → Создать токен
     - **Модульбанк**: ЛК → Настройки → API → Bearer-токен
     - **Альфа-Банк**: Альфа-Бизнес Онлайн → API → API-ключ
3. Готово! Агент может вызывать `get_bank_statement`

**Никаких логинов, никаких передач паролей — только ваш личный токен.**

## 📋 Примеры запросов

### На естественном языке для AI-агента:

- "Получи выписку по счёту за последний месяц"
- "Покажи все операции за период с 1 января по 31 января 2025"
- "Сколько денег поступило на счёт в декабре?"
- "Найди все операции с контрагентом 'ООО Рога и Копыта'"

### Прямой вызов tool:

```python
result = await get_bank_statement(
    from_date="2025-01-01",
    to_date="2025-01-31",
    account_id="1234567890"  # опционально
)
```

## 🏗️ Структура проекта

```
bank-statement-mcp/
├── mcp_instance.py          # Единый экземпляр FastMCP
├── server.py                # HTTP-сервер с streamable-http (FastMCP 2.0)
├── tools/
│   ├── get_bank_statement.py  # Единственный tool для всех банков
│   └── utils.py             # ToolResult и утилиты
├── tests/                   # Unit и интеграционные тесты
├── env_options.json         # Конфигурация для Cloud.ru
├── mcp-server-catalog.yaml  # Метаданные для каталога
├── mcp_tools.json           # Описание tools в JSON
└── Dockerfile               # Контейнеризация
```

## 🔧 Разработка

### Переменные окружения

- `MODE` — режим работы (`prod` | `test`, по умолчанию `test`)
- `HOST` — адрес HTTP сервера (по умолчанию `0.0.0.0`)
- `PORT` — порт HTTP сервера (по умолчанию `8080`)
- `BANK_PROVIDER` — выбранный банк по умолчанию: `tbank` | `modulbank` | `alfa`
- `T_BANK_TOKEN` / `MODULBANK_TOKEN` / `ALFA_TOKEN` — токен конкретного банка
- `T_BANK_SANDBOX_TOKEN` — токен песочницы T‑Bank (default `TBankSandboxToken`)

### Установка зависимостей

```bash
pip install .
```

### Запуск локально

```bash
HOST=0.0.0.0 PORT=8080 MODE=prod python server.py
```

Сервер использует FastMCP 2.0 с транспортом `streamable-http` и автоматически запускается через `mcp.run()`.

**Доступные endpoints:**
- `GET /health` - проверка состояния сервера
- `GET /` - информация о сервисе и список доступных tools
- `POST /mcp` - MCP protocol endpoint (tools/list, tools/call)

Или через Docker:

```bash
docker compose up
```

### Тестовый режим (заглушки)

```bash
MODE=test BANK_PROVIDER=tbank T_BANK_TOKEN=dummy pytest tests/
```

В режиме `MODE=test` HTTP-запросы к банкам не выполняются, возвращаются реалистичные заглушки.

#### Песочница Модульбанка

- При `MODE=test` и `BANK_PROVIDER=modulbank` tool отправляет запросы в песочницу `https://api.modulbank.ru/v1` с заголовками `sandbox: on`, `Authorization: Bearer sandboxtoken` и обязательными sandbox-ключами.
- Sandbox-ключи подтягиваются автоматически, без запроса у пользователя:
  - `MODULBANK_SANDBOX_CLIENT_ID` (default `sandboxapp`)
  - `MODULBANK_SANDBOX_CLIENT_SECRET` (default `sandboxappsecret`)
  - `MODULBANK_SANDBOX_TOKEN` (default `sandboxtoken`)
- Обязательно передавайте `account_id` — путь `/operation-history/{accountId}` требует его.
- Параметр `bank_provider` в вызове tool может переопределить `BANK_PROVIDER` из окружения.
- Пример проверки песочницы локально:
  ```bash
  MODE=test BANK_PROVIDER=modulbank \
  python -c "import asyncio; from tools.get_bank_statement import get_bank_statement; asyncio.run(get_bank_statement.fn(from_date='2024-01-01', to_date='2024-01-02', account_id='70ca00f6-1f10-4964-aca6-a5ec032efe37'))"
  ```

#### Песочница T‑Bank

- При `MODE=test` и `BANK_PROVIDER=tbank` (или `bank_provider="tbank"`) запрос идёт в `https://business.tbank.ru/openapi/sandbox/api/v1/statement`.
- Токен песочницы берётся из `T_BANK_SANDBOX_TOKEN` (default `TBankSandboxToken`), заголовок `Authorization: Bearer ...`.
- Требуется `account_id` → уходит в параметр `accountNumber`. Параметры `from/to` формируются с суффиксом `Z`.
- Ответ соответствует контракту T‑Bank sandbox: содержит `balances` и `operations`.

### Тесты

```bash
pytest tests/
```

### Линтинг

```bash
ruff check .
black .
```

## 📊 Поддерживаемые банки

| Банк | Переменная окружения | Где получить токен |
|------|---------------------|-------------------|
| T‑Bank | `T_BANK_TOKEN` | ЛК → Интеграции → API |
| Модульбанк | `MODULBANK_TOKEN` | ЛК → Настройки → API |
| Альфа-Банк | `ALFA_TOKEN` | Альфа-Бизнес Онлайн → API |

## 🔒 Безопасность

- Токены хранятся в `secretEnvs` (не логируются)
- Никаких паролей не передаётся
- Все запросы идут напрямую в API банка
- Соответствие требованиям 152-ФЗ

## 📝 Лицензия

MIT

## 🤝 Вклад

Проект создан для каталога Evolution AI Agents Cloud.ru. Вопросы и предложения приветствуются!

---

**Статус**: ✅ Готов к публикации в каталог Cloud.ru  
**Версия**: 1.0.0  
**FastMCP**: 2.0+ (streamable-http транспорт)  
**Соответствие стандарту**: 100% Cloud.ru (декабрь 2025)
