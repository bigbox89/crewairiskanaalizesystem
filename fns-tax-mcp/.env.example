# Режим работы: test (api-fns.ru) или prod (nalog.gov.ru)
FNS_MODE=test

# Токен от api-fns.ru (обязательно)
FNS_API_TOKEN=your_api_token_here

# Токен от nalog.gov.ru (только для prod режима)
FNS_PROD_TOKEN=your_prod_token_here

# Путь к файлу КЭП .p12 внутри контейнера (только для отправки)
FNS_CERT_PATH=/certs/cert.p12

# Пароль от КЭП (только для отправки)
FNS_CERT_PASSWORD=your_cert_password_here
